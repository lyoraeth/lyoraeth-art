# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Vitest coverage reports
coverage
# @nuxt/test-utils self-registers here on every `pnpm test`
.nuxtrc

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# IDE
.vscode

# Local env files
.env
.env.*
!.env.example

# OTR
darkstage-design-spec.md
concept-darkstage (trying4_i_hate_figma).html
# мне западло теперь еще и в десятый раз самому все переписывать
.claude

список фиксов.txt
юридическая залупа/
commits.txt

# Licensed typefaces — served from the VPS, never committed (see design-system.css)
/public/fonts/

# Self-hosted image variants — generated by the media:sync task, served from
# the VPS (bind-mounted to /root/lyoraeth/media in prod). Local dev writes
# here too. Source-of-truth is Sanity + media/_raw/, never this directory.
/media/

# Playwright run artifacts
/test-results/
/playwright-report/
